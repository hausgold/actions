name: Create/Update a new Release draft

on:
  workflow_call: {}

jobs:
  release_drafter:
    name: Draft Release
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: release-drafter/release-drafter@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
