name: Package Container Image

on:
  workflow_call: {}

jobs:
  package:
    name: Container Image
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - name: Prepare the environment
        uses: hausgold/actions/ci@v2
        with:
          clone_token: '${{ secrets.CLONE_TOKEN }}'
          settings_secret_key: '${{ secrets.SETTINGS_SECRET_KEY }}'
          settings: '${{ github.repository }}'
          target: ci/docker-app

      - name: Build the container image
        run: make -C config/container build

      - name: Run the container image test suite
        run: make -C config/container test

      - name: Upload the container image (AWS)
        run: make -C config/container publish

      - name: Upload the container image (Docker Hub)
        run: make -C config/container publish-prebuilt
